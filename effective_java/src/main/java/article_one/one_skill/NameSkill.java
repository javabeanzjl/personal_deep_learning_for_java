package article_one.one_skill;

/**
 * 命名规范
 *
 * @author 程序员阿乐
 * @description
 * @createDate 2025/2/17 18:28
 */
public class NameSkill {

    public static void main(String[] args) {
//        valueOf()：类型转换或返回与参数等价的对象（如Integer.valueOf(5)）。
//
//        of()：更简洁的valueOf替代（如EnumSet.of(Season.SPRING)）。
//
//        getInstance()：返回单例或特定上下文中的实例（如Calendar.getInstance()）。
//
//        newInstance()：明确返回新实例（如Class.newInstance()）。
//
//        create()/new：强调每次创建新对象（如Arrays.asList()返回的列表不可变，但ArrayList.create()可返回可变列表）。
    }
}
